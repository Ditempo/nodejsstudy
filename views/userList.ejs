<!DOCTYPE html>
<html lang="en">
    <head>
        <title></title>
        <meta charset="UTF-8">
    </head>
    <body>
        <h1>사용자 리스트</h1>
        <div>
            <ul>
                <% for(var i=0;i<length;i++){ %>
                    <!--<%= results[i]._doc.id %>
                    <%= results[i]._doc.name %>-->
                   <li> <span>아이디 : </span> <%= results[i]._doc.id %> 
                        <span>이름 : </span> <%= results[i]._doc.name %> 
                        <span>이메일 : </span> <%= results[i]._doc.email %> 
                        
                        </li>
                <% } %>
            </ul>
        </div>
    </body>
</html>